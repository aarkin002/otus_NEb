«Организация взаимодействия сегмента распределенной сети строительной компании»

<img src="/Project/img/0.PNG" alt="Alt text"/>

В кратце:

Внизу слева dhcp 2 области(настроены на маршрутизаторах выше) на правый и левый сегмент сети - разные, через VLAN, (+ssh, bpduguard, port security)

Выше 3 коммутатора LACP, STP

Выше 2 маршрутизатора HSRP (+ роутер на палочке)

В правой части NAT с перегрузкой, PAT, нат в 80 и 443 порты и ACL листы

Динамическая маршрутизация OSPF + статические маршруты в обеих частях

Реализация :

Настройка коммутатора AS - VLAN - Добавлены вланы 1000 (native), 999 (parking для не активных портов), 11 для ПК, так же настроены транки.

<img src="/Project/img1/0.PNG" alt="Alt text"/>

Настройка коммутатора AS - SSH:

<img src="/Project/img1/1.PNG" alt="Alt text"/>

Проверка работоспособности AS - SSH:

<img src="/Project/img1/2.PNG" alt="Alt text"/>

Настройка коммутатора AS - STP выбран протокол RSTP (из-за скорости) коммутатор выбран корневым для своего влан (spanning-tree vlan № root primary):

<img src="/Project/img1/3.PNG" alt="Alt text"/>

Настройка коммутатора MS - STP выбран протокол RSTP (из-за скорости) коммутатор выбран корневым для своего влан (spanning-tree vlan № root primary):

<img src="/Project/img1/4.PNG" alt="Alt text"/>

Настройка коммутатора AS - port channel LACP active (по аналогии настроены соседние коммутаторы).

<img src="/Project/img1/4-1.PNG" alt="Alt text"/>

Настройка коммутатора AS - BPDUguard и portfast в сторону конечных устройств (по аналогии настроены соседние коммутаторы).

<img src="/Project/img1/4-2.PNG" alt="Alt text"/>

<img src="/Project/img1/4-3.PNG" alt="Alt text"/>

Настройка коммутатора MS - PORT security (sticky).

<img src="/Project/img1/4-4.PNG" alt="Alt text"/>

Настройка маршрутизатора AR - DHCP - Настроена область 10.0.1.0/24 добавлены адреса из области 2.0 (в случае выхода из строя области 2.0 настроенной на маршрутизаторе MR иы настраиваем вторую область на AR и продолжаем работать), дефолт роутер для 10.0.1.0 установлен адрес "HSRP роутера" (по аналогии настроен роутер MR).

<img src="/Project/img1/5.PNG" alt="Alt text"/>

Настройка маршрутизатора AR - Роутер на палочке (11 влан, 12 влан и 1000 влан native, энкапсуляция, ip адрес. По аналогии настроен MR роутер)

<img src="/Project/img1/6.PNG" alt="Alt text"/>

Настройка маршрутизатора AR - OSPF (добалвен интерфейс g0/2 в 56 процесс зону 0(а так же интерфейсы настроенные как роутер на палочке 11 и 12 влан), задан роутер id) и статический маршрут до маршрутизатора выше

<img src="/Project/img1/7.PNG" alt="Alt text"/>

Настройка маршрутизатора AR - HSRP (выбран preempt для 11 влана (область 10.0.1.0), version 2 )

<img src="/Project/img1/8.PNG" alt="Alt text"/>

Настройка маршрутизатора AR - HSRP (выбран preempt для 12 влана (область 10.0.2.0), version 2 )

<img src="/Project/img1/9.PNG" alt="Alt text"/>

На маршрутизаторе RL (проходной) настроены интерфейсы и OSPF:

<img src="/Project/img1/10.PNG" alt="Alt text"/>

На маршрутизаторе RR настроены интерфейсы, OSPF, access-list для области 10.0.0.0 255.0.0.0, разрешающий - OSPF, доступ с 10.0.0.0 /8 до 10.0.3.0 и 4.0 /24, разрешающий доступ по 80 порту и 8080 порту, а так же запрещающий доступ хосту 10.0.2.2 по 80 порту к 176.0.4.2
Так же настроен PAT с адресом g0/1, добавлены порты g0/1 - как outside, g0/2 - как inside.

<img src="/Project/img1/11.PNG" alt="Alt text"/>

Проверка PAT

<img src="/Project/img1/11-0.PNG" alt="Alt text"/>

Проверка access-list

80 порт нет доступа у 10.0.2.2

<img src="/Project/img1/11-1.PNG" alt="Alt text"/>

8080 доступ есть у 10.0.2.2

<img src="/Project/img1/11-2.PNG" alt="Alt text"/>

80 порт есть доступ у 10.0.2.3

<img src="/Project/img1/11-3.PNG" alt="Alt text"/>

На маршрутизаторе KR настроены интерфейсы, NAT(при подключении на 80 порт 176.0.4.2 перенаправляющий на 10.5.0.2 80 порт, а так же при подключении на 8080 порт 176.0.4.2 перенаправляющий на 10.5.0.3 443 порт, g0/1 выставлен внутренним g0/2 внешним), OSPF g0/2

<img src="/Project/img1/12.PNG" alt="Alt text"/>

Проверяем OSPF:

<img src="/Project/img1/13.PNG" alt="Alt text"/>






